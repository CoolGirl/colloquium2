<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/miAdd"
        android:icon="@android:drawable/ic_menu_add"
        android:title="@string/miAdd_title"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/miStartVoting"
        android:title="@string/miStartVoting_title"
        app:showAsAction="ifRoom|withText" />

    <item
        android:visible="false"
        android:id="@+id/miEndVoting"
        android:title="@string/miEndVoting_title"
        app:showAsAction="ifRoom|withText" />

    <item
        android:visible="false"
        android:id="@+id/miReset"
        android:title="@string/miReset_title"
        app:showAsAction="ifRoom|withText" />
</menu>
